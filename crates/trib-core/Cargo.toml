[package]
name = "trib-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Core domain types and pure console state for Tributary"

[features]
# The pure crate carries no web dependency unless the daemon asks for schemas.
openapi = ["dep:utoipa"]

[dependencies]
serde.workspace = true
thiserror.workspace = true
utoipa = { workspace = true, optional = true }

[dev-dependencies]
serde_json.workspace = true
# Only to pin the field ordering `project.toml` depends on: toml refuses a
# value after a table, so a reordered MixerState would make every manifest
# unwritable. The crate itself stays serialization-format agnostic.
toml.workspace = true

[lints]
workspace = true
